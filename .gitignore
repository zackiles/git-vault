.vault/*.pw

# Keep storage tracked
!.vault/storage

# Repomix context file. Don't commit, generate locally when needed.
codebase-summary.xml

# Git-Vault test temporary files
tmp/
test/tmp/
node_modules/

# Ignore nested git directories in vendored test helpers
test/test_helper/**/.git

# Build path, only for Github Releases
bin/

.DS_Store
*.env
*.log
